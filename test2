const fs = require('fs');
const folderPath = '.';

function main(folderPath){
    const a = fs.readdirSync(folderPath);
    console.log(a);
    fs
    // findtodo(folderPath);
    // console.log(folderPath);
    // testing();
}

main(folderPath);